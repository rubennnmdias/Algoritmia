Random r= new Random();
int num = r.Next(101);
int count = 0;
Console.WriteLine("A máquina escolheu um numero entre 0 e 100, tente adivinha-lo");
string player = Console.ReadLine();

if(int.Parse(player) == num)
{
Console.WriteLine("Parabéns, acertou na primeira tentativa!");
}

else
{
  While(int.Parse(Player) != num) {
    if(int.Parse(player) > num)
    {
      Console.WriteLine("Escolheu um numero acima da máquina");
    }
    else
    {
      Console.WriteLine("Escolheu um numero abaixo da maquina");
    }
    
    count++;
    
    console.WriteLine("Tente novmente");
    player = Console.ReadLine();
    }
    Console.WriteLine("Parabéns acertou no numero que a máquina escolheu em " + (count + 1) + "tentativasa");
    }
   }
